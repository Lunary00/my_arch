[opener]
open-image = [
    { run = 'setsid imv %s', orphan = true, for = "unix" },
]
open-pdf = [
    { run = 'setsid zathura %s', orphan = true, for = "unix" },
]

[open]
prepend_rules = [
    { mime = "image/*", use = "open-image" },
    { mime = "application/pdf", use = "open-pdf" },
]
